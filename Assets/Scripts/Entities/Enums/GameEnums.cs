public enum Player { One = 1, Two = 2 };
public enum TableArea { None, Hand, Deck, Units }

public enum UnitType { None, Murlock };

public enum TargetAlignment { Friendly, Enemy }
public enum TargetType { All, Unit, Hero }